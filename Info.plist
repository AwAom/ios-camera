<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDevelopmentRegion</key>
    <string>en</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>
    <key>CFBundleDisplayName</key>
    <string>USB Camera</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>CFBundleShortVersionString</key>
    <string>1.0.0</string>
    <key>CFBundleVersion</key>
    <string>1</string>
    <key>LSRequiresIPhoneOS</key>
    <true/>

    <!-- Permissions -->
    <key>NSCameraUsageDescription</key>
    <string>This app needs access to the camera to capture and stream high-resolution video.</string>
    <key>NSMicrophoneUsageDescription</key>
    <string>This app needs access to the microphone to capture audio alongside the video stream.</string>
    <key>NSLocalNetworkUsageDescription</key>
    <string>This app hosts a local MJPEG stream so desktop applications (e.g. OBS) can view the camera feed over the network.</string>
    <key>NSBonjourServices</key>
    <array>
        <string>_usbcam._tcp</string>
    </array>

    <!-- Keep the video pipeline alive and the UI in the correct orientation -->
    <key>UIRequiresFullScreen</key>
    <true/>
    <!-- Landscape-only: this is a camera-mount/streaming use case, and
         locking to one orientation family removes the portrait/landscape
         ambiguity that was causing the preview layer's rotation to mismatch
         its actual frame (see CameraPreviewView.swift). -->
    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>
    <key>UIViewControllerBasedStatusBarAppearance</key>
    <false/>
    <key>UIStatusBarHidden</key>
    <true/>

    <!-- Allow the app to keep running audio/video background modes if plugged in for long capture sessions -->
    <key>UIBackgroundModes</key>
    <array>
        <string>audio</string>
    </array>

    <key>ITSAppUsesNonExemptEncryption</key>
    <false/>
</dict>
</plist>
